"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const vscode_1 = require("../util/vscode");
function createEditorTheme(vscodeTheme, fileName) {
    const getColor = vscode_1.findEditorColor(vscodeTheme.colors);
    return {
        name: vscodeTheme.name,
        dark: vscodeTheme.type === 'dark',
        author: vscodeTheme.author,
        editorScheme: `/themes/${fileName}.xml`,
        ui: {
            '*': {
                background: getColor(['sideBar.background']),
                foreground: getColor(['sideBar.foreground'])
            }
        }
    };
}
exports.createEditorTheme = createEditorTheme;
