"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_extra_1 = __importDefault(require("fs-extra"));
const fs_1 = require("../fs");
describe('util/fs', () => {
    describe('createTempDir', () => {
        it('should be defined', () => {
            expect(fs_1.createTempDir).toBeDefined();
        });
        it('should return the path to a temp directory', () => {
            const fsSpy = jest.spyOn(fs_extra_1.default, 'removeSync').mockImplementation(jest.fn);
            expect(fs_1.createTempDir()).toContain('code-theme-converter');
            expect(fsSpy).toHaveBeenCalled();
        });
    });
});
