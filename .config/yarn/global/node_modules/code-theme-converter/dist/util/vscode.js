"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_extra_1 = __importDefault(require("fs-extra"));
const path_1 = __importDefault(require("path"));
const json5_1 = __importDefault(require("json5"));
const ramda_1 = require("ramda");
exports.findEditorColor = ramda_1.curryN(2, function findEditorColorForField(colors = {}, fieldNames = []) {
    for (const field of fieldNames) {
        if (Object.prototype.hasOwnProperty.call(colors, field)) {
            return colors[field];
        }
        continue;
    }
    return '';
});
exports.findTokenColorForScope = ramda_1.curryN(2, function findTokenColorForScope(tokenColors = [], scope = '') {
    for (const token of tokenColors) {
        const tokenScope = token.scope;
        let isDefined = false;
        if (typeof tokenScope === 'string' && tokenScope != '') {
            isDefined = tokenScope.includes(scope);
        }
        else if (Array.isArray(tokenScope)) {
            isDefined =
                tokenScope.filter(element => element.includes(scope)).length > 0;
        }
        else {
            return null;
        }
        if (isDefined) {
            return token;
        }
    }
    return null;
});
function readCodeTheme(themeDir, theme) {
    return __awaiter(this, void 0, void 0, function* () {
        return new Promise((resolve, reject) => {
            fs_extra_1.default.readFile(path_1.default.join(themeDir, theme), (err, data) => {
                // eslint-disable-next-line @typescript-eslint/strict-boolean-expressions
                if (!err) {
                    const vscodeTheme = json5_1.default.parse(data.toString('utf8'));
                    resolve(vscodeTheme);
                }
                reject(err);
            });
        });
    });
}
exports.readCodeTheme = readCodeTheme;
function readCodeThemePackage(dir) {
    return __awaiter(this, void 0, void 0, function* () {
        return new Promise((resolve, reject) => {
            fs_extra_1.default.readFile(path_1.default.join(dir, 'package.json'), function (err, data) {
                // eslint-disable-next-line @typescript-eslint/strict-boolean-expressions
                if (!err) {
                    const packageJson = JSON.parse(data.toString('utf8'));
                    resolve(packageJson);
                }
                reject(err);
            });
        });
    });
}
exports.readCodeThemePackage = readCodeThemePackage;
